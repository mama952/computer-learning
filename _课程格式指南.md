# 计算机知识库 · 完整课程数据文件编写指南

本指南说明如何编写 `d:\computer-learning\data\<语言>-full.js` 这样的完整课程数据文件。
**请务必严格遵循本指南的格式**，否则网站会加载失败。

## 1. 文件整体结构

每个完整课程文件都以如下骨架开头和结尾：

```js
/* ============================================================
   计算机知识库 · 数据：<语言名> 完整课程（80 章，从下载安装到进阶）
   ============================================================ */
window.LANGUAGE_EXTRA = window.LANGUAGE_EXTRA || [];
window.LANGUAGE_EXTRA.push({
  id: "<语言的子项id>",        // 必须用下面第 3 节指定的 id，否则无法覆盖旧内容
  name: "<语言显示名>",
  icon: "<1-2个字符的图标>",
  tagline: "<一句话简介>",
  intro:
    "<较长的一段简介，介绍这套课程的学习路径和能学到什么>",
  meta: { 难度: "从零到进阶", 章节: "80 章", 场景: "<适用场景>" },
  lessons: [
    // ... 80 个 lesson 对象（见第 2 节）
  ]
});
```

## 2. 每个 lesson（章节）的格式

```js
{
  id: "<语言前缀>-<数字>",          // 例如 "js-1"、"js-2" … 必须全局唯一
  title: "<章节标题>",
  summary: "<一句话摘要>",
  difficulty: "入门",               // 可用：入门 / 基础 / 进阶 / 高级
  blocks: [
    // 见下方块类型
  ],
  templates: [                      // 可选：内置编辑器模板
    { name: "<模板名>", code: "<代码>" },
  ],
}
```

**id 必须全局唯一**，前缀用 `语言缩写`：JavaScript 用 `js-`，HTML/CSS 用 `hc-`，
Java 用 `jv-`，C 用 `cl-`，C++ 用 `cp-`，Go 用 `go-`，Rust 用 `rs-`，SQL 用 `sql-`，
Shell 用 `sh-`。例如 `js-7`、`hc-12`。

## 3. 各语言必须使用的子项 id（用于覆盖 languages.js 里的旧内容）

| 语言 | 子项 id | 语言缩写前缀 |
|------|---------|------------|
| JavaScript | `javascript` | `js-` |
| HTML & CSS | `htmlcss` | `hc-` |
| Java | `java` | `jv-` |
| C | `c-lang` | `cl-` |
| C++ | `cpp` | `cp-` |
| Go | `go` | `go-` |
| Rust | `rust` | `rs-` |
| SQL | `sql` | `sql-` |
| Shell / Bash | `shell` | `sh-` |

## 4. blocks 中支持的块类型（必须用这些，不能发明新类型）

| type | 用途 | 字段 |
|------|------|------|
| `p` | 段落正文 | `text` |
| `h` | 二级小标题 | `text` |
| `h3` | 三级小标题 | `text` |
| `list` | 列表（无序） | `items`（字符串数组） |
| `list` + `ordered:true` | 有序列表 | `items` |
| `tip` | 小贴士 | `title`（可选）, `text` |
| `info` | 信息说明 | `title`（可选）, `text` |
| `warn` | 警告（新手坑） | `title`（可选）, `text` |
| `danger` | 严重警告 | `title`（可选）, `text` |
| `keypoints` | 本节要点总结 | `items` |
| `code` | 代码块 | `lang`, `title`, `code` |
| `table` | 表格 | `head`（数组）, `rows`（二维数组） |

**代码块示例**：

```js
{ type: "code", lang: "javascript", title: "第一个程序", code: 'console.log("Hello, World!");\nlet name = "小明";\nconsole.log("你好，" + name);' }
```

**code 字符串中的换行**：必须用 `\n` 转义符写在单行里，**禁止在字符串里写真实换行**。
如果代码里有双引号，字符串外层用单引号包住；如果代码里有单引号，外层用双引号。
```js
// 正确：外层单引号，内部双引号
code: 'print("hello")'
// 正确：外层双引号，内部单引号
code: "print('hello')"
```

**正文里的行内代码 / 链接**：
- 行内代码用 `<code.inline>print()</code.inline>` 这种 HTML 标签。
- 加粗用 `**文字**`。
- 下载链接用 `<a href='https://xxx' target='_blank' rel='noopener'>文字</a>`（注意外层是单引号）。

## 5. 内容要求（非常重要）

1. **总共 80 章**，从「下载与安装」讲起（第 1 章必须是下载安装，用真实官网下载链接），
   一直讲到进阶/实战/项目/面试，难度递增。
2. **每章 6~12 个 blocks**，正文用大白话，把每个概念、每个用法、每个坑都讲透，
   宁可啰嗦，不要精简。目标是教会小白。
3. **每章至少 1 个 code 块**，能示例的一定给可运行示例。
4. **常见错误/坑** 用 `warn` 或 `danger` 块突出。
5. **每章结尾用 `keypoints` 总结本节要点**。
6. `templates` 尽量提供 1~3 个可运行的模板（每章或每几章一个）。
7. 下载链接一律用官网、且完全免费的软件。
8. **不要使用任何 Markdown 表格语法**；表格必须用 `table` 块。

## 6. 写完后必须验证

写完文件后，在终端运行（PowerShell）：

```powershell
node "d:\computer-learning\_check.js" "d:\computer-learning\data\<文件名>.js"
```

如果输出 `语法 OK` 且 `章节=80`，说明文件合格。如果报错，修复到通过为止。

## 7. 参考样例（一个完整章节）

下面是从 Python 完整课程中摘取的完整章节样例，格式可完全照抄：

```js
{
  id: "python-1",
  title: "下载与安装 Python（零基础第一步）",
  summary: "到官方下载、勾选 PATH、验证版本，三步装好 Python。",
  difficulty: "入门",
  blocks: [
    { type: "p", text: "学习 Python 的第一步，是把它装到你的电脑上。别担心，这一步非常简单，跟着做就能完成。Python 官方提供完全免费的安装包，支持 Windows、macOS、Linux 三大系统。" },
    { type: "h", text: "第一步：下载" },
    { type: "list", items: ["打开官网下载页：<a href='https://www.python.org/downloads/' target='_blank' rel='noopener'>https://www.python.org/downloads/</a>", "点击黄色大按钮『Download Python 3.x.x』（3.x.x 是版本号，选最新的稳定版即可）", "Windows 用户下载 64-bit installer（64 位安装包）"] },
    { type: "h", text: "第二步：安装（最关键的一步）" },
    { type: "p", text: "双击下载好的安装包，**第一屏一定要勾选最底部的『Add python.exe to PATH』**，这一步非常重要！如果不勾，之后在命令行输入 python 会提示找不到命令。勾选后再点『Install Now』，然后等待安装完成即可。" },
    { type: "warn", title: "新手第一大坑", text: "很多新手装完发现命令行输入 python 没反应，原因就是忘了勾『Add python.exe to PATH』。如果遇到这种情况，卸载重装一遍，记住勾选即可。" },
    { type: "h", text: "第三步：验证安装" },
    { type: "code", lang: "python", title: "验证是否装好", code: 'print("Hello, Python!")\nprint("安装成功，我开始学编程了！")' },
    { type: "p", text: "在电脑上按 <code.inline>Win+R</code.inline>，输入 <code.inline>cmd</code.inline> 回车打开命令行，输入 <code.inline>python --version</code>，如果显示类似 <code.inline>Python 3.12.x</code> 的版本号，就说明安装成功了！" },
    { type: "tip", title: "顺手装个好编辑器", text: "推荐安装 VS Code（完全免费）：<a href='https://code.visualstudio.com' target='_blank' rel='noopener'>code.visualstudio.com</a>。装好后在扩展商店搜『Python』并安装官方插件，写代码就有自动补全了。" },
    { type: "keypoints", items: ["官方下载地址是 python.org/downloads，完全免费", "安装时务必勾选 Add python.exe to PATH", "cmd 里运行 python --version 验证是否装好", "推荐配合 VS Code + Python 插件使用"] },
  ],
  templates: [
    { name: "我的第一个程序", code: 'print("Hello, Python!")\nprint("我要学会编程！")' },
  ],
},
```
