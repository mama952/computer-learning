/* ============================================================
   计算机知识库 · 数据：TypeScript 完整课程（16 章，从入门到进阶）
   ============================================================ */
window.LANGUAGE_EXTRA = window.LANGUAGE_EXTRA || [];
window.LANGUAGE_EXTRA.push({
  id: "typescript",
  name: "TypeScript",
  icon: "TS",
  tagline: "JavaScript 的超集，类型系统让大型前端项目安全可维护。",
  intro:
    "这是一套从零开始的 TypeScript 完整课程。第 1 章讲清 TS 与 JS 的关系和类型系统的价值，第 2 章教你安装 Node.js 与 TypeScript，之后从编译流程、基础类型、接口、类型别名一路学到函数、类、泛型、类型收窄、模块化、内置工具类型，再进入 React、Express 实战，最后一章讲工程化配置与常见排错。每一章都用大白话讲透概念和坑，配可运行示例代码与编辑器模板。",
  meta: { 难度: "从零到进阶", 章节: "16 章", 场景: "前端/全栈/大型项目" },
  lessons: [
    {
      id: "ts-1",
      title: "TypeScript 是什么：为什么前端都要用它",
      summary: "认识 JS 超集、类型系统的价值，理解 TS 最终要编译成 JS。",
      difficulty: "入门",
      blocks: [
        { type: "p", text: "TypeScript（简称 TS）是 JavaScript（JS）的**超集**：所有合法的 JS 代码都是合法的 TS 代码，TS 在 JS 的基础上增加了**类型系统**。它由微软开发，2012 年发布，如今 React、Vue、Angular 等主流前端项目几乎都在用。" },
        { type: "h", text: "JS 的问题与 TS 的答案" },
        { type: "p", text: "JS 是动态类型语言，变量可以随便改类型，这带来了灵活，也埋下了坑：函数接收了一个写错类型的参数，往往要等程序运行到那一行才报错，甚至到用户电脑上才崩。**TS 在写代码时（编译期）就做静态检查**，类型不对编辑器立刻标红，把 bug 扼杀在摇篮里。" },
        { type: "code", lang: "typescript", title: "有类型 vs 没类型", code: '// JS：不知道参数是什么类型，出错要到运行时\nfunction add(a, b) {\n  return a + b;\n}\nconsole.log(add("hello", 2));  // 运行时变成字符串拼接 hello2，不报错但结果错\n\n// TS：明确标注类型，传错类型编辑器直接报红\nfunction tsAdd(a: number, b: number): number {\n  return a + b;\n}\n// console.log(tsAdd("hello", 2));  // 编译错误：类型不匹配' },
        { type: "p", text: "上面 <code.inline>tsAdd</code.inline> 的参数 <code.inline>a: number</code.inline> 表示 a 必须是数字。你如果传字符串进去，VS Code 里会立刻划红波浪线，编译也会报错。这就是类型系统的价值：**错误更早被发现，代码自带文档**。" },
        { type: "h", text: "TS 最终会被编译成 JS" },
        { type: "code", lang: "typescript", title: "TS 编译成 JS", code: '// 这是 .ts 文件里写的：\nlet age: number = 20;\nconst name: string = "小明";\n\nfunction greet(person: string): string {\n  return "你好，" + person;\n}\nconsole.log(greet(name));' },
        { type: "p", text: "浏览器和 Node.js **只能运行 JS**，不能直接运行 TS。所以写完 TS 后要用编译器（tsc）把它**编译成纯 JS**。编译时会擦掉所有类型标注，生成一份干净的 JS 文件。所以 TS 的完整工作流是：写 TS → tsc 编译 → 得到 JS → 交给浏览器/Node 运行。" },
        { type: "table", head: ["对比", "JavaScript", "TypeScript"], rows: [["类型", "动态，运行才知道对错", "静态，写的时候就能检查"], ["运行", "浏览器/Node 直接跑", "先编译成 JS 再跑"], ["上手", "简单灵活", "多一套类型语法"], ["大型项目", "难维护", "更安全，重构更放心"]] },
        { type: "keypoints", items: ["TS 是 JS 的超集，合法 JS 也是合法 TS", "TS 增加类型系统，编译期就能发现错误", "浏览器/Node 只能跑 JS，TS 必须先用 tsc 编译", "主流前端（React/Vue）都大量使用 TS"] },
      ],
      templates: [
        { name: "TS 与 JS 对比", code: '// JS 写法\nfunction add(a, b) { return a + b; }\n\n// TS 写法：参数和返回值都标了类型\nfunction addTS(a: number, b: number): number { return a + b; }\n\nconsole.log(addTS(3, 5)); // 8\n// console.log(addTS("3", 5)); // 这行在 TS 里会报错' },
      ],
    },
    {
      id: "ts-2",
      title: "下载与安装：Node.js + TypeScript",
      summary: "先装 Node.js，再用 npm 全局安装 TypeScript，验证 tsc。",
      difficulty: "入门",
      blocks: [
        { type: "p", text: "TypeScript 编译器（tsc）是用 Node.js 编写的，所以第一步是先装 Node.js（它会顺带带上 npm 包管理器），然后用 npm 安装 TypeScript。整个流程完全免费、跨平台。" },
        { type: "h", text: "第一步：安装 Node.js" },
        { type: "list", items: ["打开 Node.js 官网：<a href='https://nodejs.org' target='_blank' rel='noopener'>https://nodejs.org</a>", "下载 LTS（长期支持）版本，LTS 更稳定，双击安装一路『下一步』", "装完在命令行运行 <code.inline>node --version</code> 和 <code.inline>npm --version</code>，能打印版本号就成功"] },
        { type: "h", text: "第二步：用 npm 安装 TypeScript" },
        { type: "code", lang: "typescript", title: "全局安装与验证", code: 'npm install -g typescript\n# 安装完成后验证版本\ntsc --version\n# 输出类似 Version 5.x.x，说明装好了' },
        { type: "p", text: "<code.inline>-g</code.inline> 表示全局安装，装好后在任何文件夹都能用 <code.inline>tsc</code.inline> 命令。如果以后想在某个项目里单独固定版本，也可以不用 -g，在项目里 <code.inline>npm install --save-dev typescript</code.inline> 局部安装。" },
        { type: "warn", title: "命令找不到怎么办", text: "Windows 上如果提示『tsc 不是内部或外部命令』，先确认 Node.js 装好了（node --version 有输出），然后**重新打开一个命令行窗口**再试——PATH 环境变量在旧窗口里不会自动刷新。" },
        { type: "h", text: "第三步：装一个好编辑器" },
        { type: "list", items: ["VS Code（完全免费，TS 官方推荐的编辑器）：<a href='https://code.visualstudio.com' target='_blank' rel='noopener'>https://code.visualstudio.com</a>", "VS Code 内置了完整的 TypeScript 支持，不需要额外装插件就能高亮、补全、报错", "JetBrains 家的 WebStorm / IDEA 也内置 TS 支持（有付费版，非必须）"] },
        { type: "tip", title: "为什么推荐 VS Code", text: "VS Code 本身就是微软用 TS 生态构建的，对 TS 的支持是原生顶级的。装好后新建 .ts 文件，写代码时类型错误会实时标红，悬停还能看到变量类型提示，体验非常好。" },
        { type: "keypoints", items: ["先装 Node.js（官网 nodejs.org，选 LTS）", "npm install -g typescript 全局安装", "tsc --version 验证是否装好", "VS Code 原生支持 TS，推荐使用"] },
      ],
      templates: [
        { name: "验证 TS 环境", code: 'const greeting: string = "TypeScript 环境 OK！";\nconsole.log(greeting);\n// 在终端用 tsc 文件名.ts 编译后用 node 运行' },
      ],
    },
    {
      id: "ts-3",
      title: "第一个程序与编译流程：tsc / tsconfig.json",
      summary: "写一个 .ts 文件，用 tsc 编译成 .js，再用 node 运行。",
      difficulty: "入门",
      blocks: [
        { type: "p", text: "装好环境后，我们写第一个 TypeScript 程序。核心流程就三步：写 <code.inline>.ts</code.inline> 文件 → 用 <code.inline>tsc</code.inline> 编译成 <code.inline>.js</code.inline> → 用 <code.inline>node</code.inline> 运行生成的 JS。记住：TS 本身不能被 Node 直接执行。" },
        { type: "h", text: "写代码并编译" },
        { type: "code", lang: "typescript", title: "完整流程", code: '// 1. 新建 hello.ts，写入：\nlet message: string = "你好，TypeScript！";\nconsole.log(message);\n\n// 2. 在命令行编译：\n// tsc hello.ts\n\n// 3. 会生成一个 hello.js 文件（类型标注已被擦除）\n// 4. 用 Node 运行：\n// node hello.js\n// 输出：你好，TypeScript！' },
        { type: "p", text: "编译后同目录会出现一个 <code.inline>hello.js</code.inline>。打开它你会发现里面是 <code.inline>var message = \"你好，TypeScript！\"</code.inline>——类型标注 <code.inline>: string</code.inline> 已经被擦掉了。这就是 TS 编译的本质：**检查类型，然后擦掉类型**。" },
        { type: "h", text: "tsconfig.json：项目配置文件" },
        { type: "code", lang: "typescript", title: "tsc --init 生成配置", code: 'tsc --init\n# 生成 tsconfig.json，里面是默认配置和注释\n\n# 然后可以简写：直接编译整个项目\n# tsc\n\n# 监听模式：文件一改就自动编译\n# tsc -w' },
        { type: "p", text: "<code.inline>tsc --init</code.inline> 会生成 <code.inline>tsconfig.json</code.inline>，告诉编译器：编译哪些文件、输出到哪里、用什么版本语法等。有了它之后，直接敲 <code.inline>tsc</code.inline> 就会按配置编译整个项目，不用每次指定文件名。" },
        { type: "h", text: "几个常用配置项" },
        { type: "code", lang: "typescript", title: "tsconfig.json 常用字段", code: '{\n  "compilerOptions": {\n    "target": "ES2020",        // 编译成哪个 JS 版本\n    "module": "commonjs",      // 模块系统\n    "outDir": "./dist",        // 编译输出到 dist 文件夹\n    "rootDir": "./src",        // 源码在 src 文件夹\n    "strict": true             // 开启严格检查（强烈建议）\n  },\n  "include": ["src"]           // 编译 src 文件夹下所有文件\n}' },
        { type: "warn", title: "strict 一定要开", text: "<code.inline>strict: true</code.inline> 会开启一系列严格检查（包括不能隐式使用 any），刚开始可能报错多一些，但能逼你写出更安全的代码。新项目建议一上来就开 strict，别图省事关掉。" },
        { type: "keypoints", items: ["流程：写 .ts → tsc 编译 → 得到 .js → node 运行", "编译本质是检查类型然后擦掉类型标注", "tsc --init 生成 tsconfig.json 项目配置", "outDir/rootDir 控制输入输出，strict 建议开启"] },
      ],
      templates: [
        { name: "我的第一个 TS 程序", code: 'let name: string = "小明";\nlet age: number = 20;\nconsole.log(`我叫${name}，今年${age}岁`);\nconsole.log("学习 TypeScript 的第一天！");' },
      ],
    },
    {
      id: "ts-4",
      title: "基础类型：number/string/boolean/元组/枚举",
      summary: "掌握 TS 的常用基础类型与 any/unknown/void/never 特殊类型。",
      difficulty: "基础",
      blocks: [
        { type: "p", text: "TS 在 JS 的基础上给数据加上了类型标注。这一章我们把最常用的基础类型过一遍，包括数字、字符串、布尔、数组、元组、枚举，以及几个特殊类型。" },
        { type: "h", text: "基本类型与数组" },
        { type: "code", lang: "typescript", title: "基本类型声明", code: 'let count: number = 42;        // 数字（含整数和小数）\nlet price: number = 3.14;\nlet title: string = "学习 TS";  // 字符串\nlet isDone: boolean = true;      // 布尔值\n\n// 数组的两种写法\nlet nums: number[] = [1, 2, 3];\nlet words: Array<string> = ["a", "b"];\n\n// 对象\nlet person: { name: string; age: number } = { name: "小明", age: 20 };\nconsole.log(count, price, title, isDone);' },
        { type: "p", text: "数组声明有两种等价写法：<code.inline>number[]</code.inline> 和 <code.inline>Array<number></code.inline>，表示『一个数组，里面每个元素都是数字』。如果往 <code.inline>nums</code.inline> 里 push 一个字符串，编辑器会立刻报红。" },
        { type: "h", text: "元组 Tuple 与枚举 Enum" },
        { type: "code", lang: "typescript", title: "元组和枚举", code: '// 元组：固定长度、每位类型固定的数组\nlet pair: [string, number] = ["张三", 25];\nconsole.log(pair[0], pair[1]);\n\n// 枚举：给一组常量起名字\nenum Color {\n  Red,\n  Green,\n  Blue,\n}\nlet c: Color = Color.Green;\nconsole.log(c);                    // 1（默认从 0 开始编号）\nconsole.log(Color[1]);             // "Green" 反查名字\n\n// 也可以手动指定值\nenum Status {\n  Pending = "pending",\n  Done = "done",\n}' },
        { type: "h", text: "特殊类型：any / unknown / void / never" },
        { type: "code", lang: "typescript", title: "特殊类型", code: 'let anything: any = 42;      // any：关闭类型检查，慎用\nanything = "随意";\n\nlet someValue: unknown = 42; // unknown：不知道类型，用前必须收窄\n// someValue.toUpperCase();   // 报错：unknown 不能直接用\n\nfunction log(msg: string): void {   // void：无返回值\n  console.log(msg);\n}\n\nfunction throwErr(): never {        // never：永远不返回\n  throw new Error("出错啦");\n}' },
        { type: "danger", title: "any 是万恶之源", text: "<code.inline>any</code.inline> 会关闭该变量的所有类型检查，等于退回纯 JS，是 TS 里最应该避免的类型。遇到确实不知道类型的情况，优先用 <code.inline>unknown</code.inline>（它强制你先收窄再用），实在没办法才用 any，并尽量用 eslint 禁止 any。" },
        { type: "warn", title: "类型标注怎么写都行？", text: "<code.inline>let a: number = 1</code.inline> 当然可以，但更推荐 <code.inline>let a = 1</code.inline> 让 TS 自己推断。类型推断是 TS 的核心能力，只有推断不出来或推断结果不对时才需要手写标注。" },
        { type: "keypoints", items: ["基础类型：number/string/boolean，数组用 number[]", "元组固定长度和每位的类型", "枚举给常量组起名，可自动或手动编号", "any 关闭检查要慎用，unknown 安全但要用前收窄", "void 无返回值，never 永不返回"] },
      ],
      templates: [
        { name: "个人信息对象", code: 'let person: { name: string; age: number; hobbies: string[] } = {\n  name: "小红",\n  age: 9,\n  hobbies: ["画画", "唱歌"],\n};\nconsole.log(`${person.name} 今年 ${person.age} 岁`);\nconsole.log(`爱好：${person.hobbies.join("、")}`);' },
      ],
    },
    {
      id: "ts-5",
      title: "接口 interface：定义对象的形状",
      summary: "用 interface 描述对象结构，掌握可选、只读与接口扩展。",
      difficulty: "基础",
      blocks: [
        { type: "p", text: "对象在 JS 里很自由，但 TS 希望它『长成规定好的样子』。**接口（interface）** 就是用来描述一个对象『应该长什么样』的：有哪些属性、各是什么类型。它让数据有了明确的形状，传错就报错。" },
        { type: "h", text: "定义接口与使用" },
        { type: "code", lang: "typescript", title: "Person 接口", code: 'interface Person {\n  name: string;\n  age: number;\n}\n\nfunction greet(p: Person): string {\n  return `你好，我叫${p.name}，今年${p.age}岁`;\n}\n\nconst xiaoming: Person = { name: "小明", age: 20 };\nconsole.log(greet(xiaoming));\n\n// 传少了属性会报错\n// greet({ name: "小明" });   // 缺少 age 属性\n\n// 传多了属性也会报错\n// greet({ name: "小明", age: 20, extra: true });  // extra 不存在' },
        { type: "p", text: "把 <code.inline>Person</code.inline> 当作一个类型用：声明变量、标注函数参数都可以。TS 做的是**结构类型检查**——只要对象结构对得上就通过，不需要显式实现某个接口（和 Java 的『鸭子类型』思路类似）。" },
        { type: "h", text: "可选属性与只读属性" },
        { type: "code", lang: "typescript", title: "? 和 readonly", code: 'interface User {\n  name: string;\n  age?: number;        // ? 表示可选：可以有，也可以没有\n  readonly id: number; // readonly 只读：初始化后不能改\n}\n\nconst u1: User = { name: "小明", id: 1 };   // 省略了 age，合法\nconst u2: User = { name: "小红", age: 9, id: 2 };\n\n// u2.id = 99;   // 报错：readonly 属性不能修改\nconsole.log(u1.name, u2.age);' },
        { type: "p", text: "属性名后面加 <code.inline>?</code.inline> 表示**可选**，有它就合法，没有也合法。<code.inline>readonly</code.inline> 表示**只读**，创建之后就不能再改，适合 id 这种不该变的数据。" },
        { type: "h", text: "扩展接口 extends" },
        { type: "code", lang: "typescript", title: "接口继承", code: 'interface Animal {\n  name: string;\n}\n\ninterface Dog extends Animal {\n  bark(): void;   // 方法：会叫\n}\n\nconst dog: Dog = {\n  name: "旺财",\n  bark() {\n    console.log("汪汪！");\n  },\n};\n\nconsole.log(dog.name);\ndog.bark();' },
        { type: "warn", title: "接口 vs 类型别名，先用哪个", text: "interface 和 type 都能描述对象类型。经验法则：**定义对象形状、类实现的契约用 interface**；联合类型、交叉类型、原始类型别名用 type（下一章讲）。两者大部分场景可以互换，别纠结。" },
        { type: "keypoints", items: ["interface 描述对象形状：有哪些属性、什么类型", "结构类型检查：结构对得上就合法", "? 表示可选属性，readonly 表示只读属性", "extends 让接口继承另一个接口", "对象形状用 interface，联合/交叉用 type"] },
      ],
      templates: [
        { name: "商品对象", code: 'interface Product {\n  name: string;\n  price: number;\n  inStock: boolean;\n}\n\nconst phone: Product = {\n  name: "手机",\n  price: 1999,\n  inStock: true,\n};\nconsole.log(`${phone.name} 售价 ${phone.price} 元`);\nconsole.log(phone.inStock ? "有货" : "缺货");' },
      ],
    },
    {
      id: "ts-6",
      title: "类型别名 type 与联合/交叉类型",
      summary: "给类型起别名，用 | 和 & 组合类型，理解字面量类型。",
      difficulty: "基础",
      blocks: [
        { type: "p", text: "除了 interface，TS 还提供 **类型别名（type）** 来给类型起名字。它最大的威力在于可以组合出**联合类型（|）**、**交叉类型（&）**和**字面量类型**，让类型的表达能力大大增强。" },
        { type: "h", text: "类型别名与联合类型" },
        { type: "code", lang: "typescript", title: "type 与 | 联合", code: 'type ID = number | string;   // 可以是数字，也可以是字符串\n\ntype Status = "pending" | "done" | "failed";  // 字面量联合：只能这三个值\n\nlet id1: ID = 123;\nlet id2: ID = "abc";\n\nlet s: Status = "done";\n// s = "unknown";   // 报错：不是合法值\n\nfunction handle(id: ID, status: Status) {\n  console.log(`处理 ${id}，状态 ${status}`);\n}\nhandle(1, "pending");' },
        { type: "p", text: "联合类型用 <code.inline>|</code.inline> 表示『或』：ID 类型要么是 number 要么是 string。<code.inline>\"pending\" | \"done\" | \"failed\"</code.inline> 是**字面量类型联合**，把变量的取值限定在这几个字符串里，能有效防止传错状态值。" },
        { type: "h", text: "交叉类型" },
        { type: "code", lang: "typescript", title: "& 交叉合并", code: 'interface HasName {\n  name: string;\n}\ninterface HasAge {\n  age: number;\n}\n\ntype Person = HasName & HasAge;   // 同时拥有 name 和 age\n\nconst p: Person = {\n  name: "小明",\n  age: 20,\n};\nconsole.log(`${p.name} ${p.age}岁`);\n\n// 交叉类型常用在给对象补字段\n// type CompleteUser = BasicUser & { isAdmin: boolean };' },
        { type: "p", text: "交叉类型用 <code.inline>&</code.inline> 表示『并且』：结果类型要同时满足所有组成部分。常用于把多个接口合并成一个完整类型。" },
        { type: "h", text: "type 与 interface 的关键区别" },
        { type: "table", head: ["对比", "interface", "type"], rows: [["对象形状", "擅长，可 extends", "可以，用 & 组合"], ["联合/交叉/字面量", "做不到", "核心能力"], ["同名合并", "可以（自动合并）", "不可以（报错）"], ["使用建议", "定义对象/类契约", "定义别名、组合类型"]] },
        { type: "warn", title: "别把 | 当成默认值", text: "<code.inline>type T = string | undefined</code.inline> 表示 T 可能是 string 也可能是 undefined，**不是**给 string 设默认值。区别默认值和可选值是新手常困惑的点：可选属性 <code.inline>?</code.inline> 才是『可以没有』。" },
        { type: "keypoints", items: ["type 给类型起别名，功能比 interface 更广", "| 联合：满足其中一个类型即可", "& 交叉：同时满足所有组成部分", "字面量类型把取值限定为固定几个值", "对象形状用 interface，组合类型用 type"] },
      ],
      templates: [
        { name: "订单状态机", code: 'type OrderStatus = "created" | "paid" | "shipped" | "done";\n\nfunction nextStatus(s: OrderStatus): OrderStatus {\n  switch (s) {\n    case "created": return "paid";\n    case "paid": return "shipped";\n    case "shipped": return "done";\n    case "done": return "done";\n  }\n}\n\nlet current: OrderStatus = "created";\nconsole.log(`当前：${current}，下一步：${nextStatus(current)}`);' },
      ],
    },
    {
      id: "ts-7",
      title: "函数类型：参数、返回值与函数签名",
      summary: "给函数标注参数和返回类型，掌握可选/默认/剩余参数。",
      difficulty: "基础",
      blocks: [
        { type: "p", text: "函数是 JS 的核心。TS 给函数加了类型标注：每个参数什么类型、返回什么类型，都写得清清楚楚。这样调用时传错参数立刻报错，代码也能自我说明。" },
        { type: "h", text: "参数类型与返回类型" },
        { type: "code", lang: "typescript", title: "基础函数标注", code: '// 参数和返回值都标注类型\nfunction add(a: number, b: number): number {\n  return a + b;\n}\nconsole.log(add(3, 5));   // 8\n\n// 无返回值用 void\nfunction log(msg: string): void {\n  console.log(msg);\n}\n\n// 箭头函数同样可以标注\nconst multiply = (a: number, b: number): number => a * b;\nconsole.log(multiply(4, 5));   // 20' },
        { type: "h", text: "可选、默认、剩余参数" },
        { type: "code", lang: "typescript", title: "三种参数写法", code: '// 可选参数：加 ?，可传可不传\nfunction greet(name: string, title?: string): string {\n  return title ? `${title}${name}` : name;\n}\nconsole.log(greet("小明", "同学："));\nconsole.log(greet("小红"));\n\n// 默认参数：给默认值\nfunction repeat(msg: string, times: number = 2): string {\n  return msg.repeat(times);\n}\nconsole.log(repeat("哈"));     // 哈哈\n\n// 剩余参数：...args 收集所有剩余参数\nfunction sumAll(...nums: number[]): number {\n  return nums.reduce((acc, n) => acc + n, 0);\n}\nconsole.log(sumAll(1, 2, 3, 4));   // 10' },
        { type: "p", text: "注意顺序：**可选参数和默认参数必须放在必选参数的后面**。<code.inline>...</code.inline> 剩余参数会把传入的所有参数收集成一个数组，数量不限。" },
        { type: "h", text: "函数类型签名" },
        { type: "code", lang: "typescript", title: "把函数本身当类型", code: '// 用类型别名描述一个函数的形状\n// 表示：接收两个 number，返回 number 的函数\ntype BinaryFn = (a: number, b: number) => number;\n\nconst addFn: BinaryFn = (a, b) => a + b;\nconst subFn: BinaryFn = (a, b) => a - b;\n\nconsole.log(addFn(10, 5));\nconsole.log(subFn(10, 5));\n\n// 回调函数参数\nfunction calc(a: number, b: number, op: BinaryFn): number {\n  return op(a, b);\n}\nconsole.log(calc(6, 2, (x, y) => x * y));' },
        { type: "info", title: "函数签名 vs 箭头函数", text: "函数签名 <code.inline>(a: number, b: number) => number</code.inline> 是『函数这个类型』的写法，和箭头函数容易混淆。记住：在**类型位置**写这个，表示『这样一个函数』；在**值位置**用箭头 <code.inline>=></code.inline> 才是真正定义函数。" },
        { type: "warn", title: "函数没返回值却用了返回类型", text: "如果函数体里没有任何 return，返回类型必须标 <code.inline>void</code.inline>，标 <code.inline>number</code.inline> 会报错。反过来，声明了 <code.inline>void</code.inline> 却在某条分支 return 了值，同样报错。" },
        { type: "keypoints", items: ["参数和返回值都要标注类型，无返回用 void", "? 可选参数、默认值参数、...剩余参数", "可选/默认参数要放必选参数后面", "函数类型签名：(a: number) => number", "回调函数用函数类型标注最方便"] },
      ],
      templates: [
        { name: "计算器函数", code: 'type Operator = (a: number, b: number) => number;\n\nconst add: Operator = (a, b) => a + b;\nconst subtract: Operator = (a, b) => a - b;\nconst multiply: Operator = (a, b) => a * b;\n\nfunction run(a: number, b: number, op: Operator): number {\n  return op(a, b);\n}\n\nconsole.log(run(10, 3, add));\nconsole.log(run(10, 3, subtract));\nconsole.log(run(10, 3, multiply));' },
      ],
    },
    {
      id: "ts-8",
      title: "类与面向对象：public/private/readonly/abstract",
      summary: "用类组织代码，掌握访问修饰符、getter/setter 与抽象类。",
      difficulty: "进阶",
      blocks: [
        { type: "p", text: "TS 的类基于 ES6 的 class 语法，并扩展了访问修饰符、readonly、getter/setter、抽象类等能力，让它成为真正完整的面向对象语言。这一章我们系统学习。" },
        { type: "h", text: "类的基础与访问修饰符" },
        { type: "code", lang: "typescript", title: "类与 public/private", code: 'class Person {\n  public name: string;      // public：外部可访问（默认）\n  private age: number;      // private：只有类内部能访问\n  readonly id: number;      // readonly：只读，初始化后不可改\n\n  constructor(name: string, age: number, id: number) {\n    this.name = name;\n    this.age = age;\n    this.id = id;\n  }\n\n  public getInfo(): string {\n    return `${this.name}，年龄 ${this.age}（对外只读 id: ${this.id}）`;\n  }\n}\n\nconst p = new Person("小明", 20, 1);\nconsole.log(p.name);          // 小明\n// console.log(p.age);        // 报错：private 不能从外部访问\nconsole.log(p.getInfo());' },
        { type: "p", text: "<code.inline>public</code.inline>（默认）外部随便访问；<code.inline>private</code.inline> 只有类内部能用，保护内部数据；<code.inline>protected</code.inline>（下节讲）允许子类访问；<code.inline>readonly</code.inline> 只读。这些修饰符让类的封装更严密。" },
        { type: "h", text: "getter / setter" },
        { type: "code", lang: "typescript", title: "属性访问器", code: 'class Account {\n  private _balance: number = 0;\n\n  // getter：读取时执行\n  get balance(): number {\n    return this._balance;\n  }\n\n  // setter：赋值时执行，可以加校验\n  set balance(value: number) {\n    if (value < 0) {\n      throw new Error("余额不能为负数");\n    }\n    this._balance = value;\n  }\n}\n\nconst acc = new Account();\nacc.balance = 100;          // 触发 setter\nconsole.log(acc.balance);   // 触发 getter，输出 100\n// acc.balance = -5;        // 报错：余额不能为负数' },
        { type: "p", text: "用 <code.inline>get</code.inline> / <code.inline>set</code.inline> 可以像访问普通属性一样读写，但内部可以藏校验逻辑。外部写 <code.inline>acc.balance = 100</code.inline> 时实际走的是 setter 方法。" },
        { type: "h", text: "抽象类 abstract" },
        { type: "code", lang: "typescript", title: "抽象类与继承", code: 'abstract class Shape {\n  abstract area(): number;   // 抽象方法：只声明，子类必须实现\n\n  describe(): string {       // 普通方法：子类直接继承\n    return `面积是 ${this.area()}`;\n  }\n}\n\nclass Circle extends Shape {\n  constructor(public radius: number) {\n    super();\n  }\n  area(): number {\n    return Math.PI * this.radius * this.radius;\n  }\n}\n\nconst c = new Circle(2);\nconsole.log(c.describe());   // 面积是 12.566...' },
        { type: "p", text: "<code.inline>abstract</code.inline> 类不能被 new，只能被继承。抽象方法只有签名没有实现，子类必须实现它。这用来约定『所有子类都一定有这个方法』，比如所有图形都有面积。" },
        { type: "warn", title: "构造参数属性简写", text: "上面 <code.inline>constructor(public radius: number)</code.inline> 里直接写 <code.inline>public</code.inline>，是 TS 的简写：自动声明同名字段并赋值。新手可能看不懂，但它是很常见的写法，记住『参数前加修饰符 = 声明字段并赋值』即可。" },
        { type: "keypoints", items: ["public 外部可访问、private 类内私有、protected 子类可用", "readonly 只读属性", "get/set 访问器可以藏校验逻辑", "abstract 类不能被 new，抽象方法必须实现", "构造函数参数前加修饰符可简写声明字段"] },
      ],
      templates: [
        { name: "银行账户类", code: 'class BankAccount {\n  private balance: number = 0;\n\n  deposit(amount: number): void {\n    if (amount <= 0) throw new Error("存款必须大于 0");\n    this.balance += amount;\n  }\n\n  withdraw(amount: number): void {\n    if (amount > this.balance) throw new Error("余额不足");\n    this.balance -= amount;\n  }\n\n  getBalance(): number {\n    return this.balance;\n  }\n}\n\nconst account = new BankAccount();\naccount.deposit(1000);\naccount.withdraw(300);\nconsole.log("余额：" + account.getBalance());' },
      ],
    },
    {
      id: "ts-9",
      title: "泛型 Generics：让类型参数化",
      summary: "用泛型写『放之四海而皆准』的函数和类，掌握 extends 约束。",
      difficulty: "进阶",
      blocks: [
        { type: "p", text: "写一个函数，它既能处理数字数组，又能处理字符串数组——如果写死类型就得复制两份代码。**泛型（Generics）** 就是解决这个问题的：用一个**类型参数 T** 代表『未知的类型』，让函数/类支持任意类型，同时仍然保持类型安全。" },
        { type: "h", text: "泛型函数" },
        { type: "code", lang: "typescript", title: "identity 泛型函数", code: '// T 是类型参数：调用时自动推断具体类型\nfunction identity<T>(value: T): T {\n  return value;\n}\n\nconsole.log(identity<number>(42));     // 显式指定 T 是 number\nconsole.log(identity("hello"));        // 自动推断 T 是 string\n\n// 泛型对数组很常用：翻转数组\nfunction reverse<T>(arr: T[]): T[] {\n  return arr.slice().reverse();\n}\nconsole.log(reverse([1, 2, 3]));       // [3, 2, 1]\nconsole.log(reverse(["a", "b"]));      // ["b", "a"]' },
        { type: "p", text: "<code.inline>identity<T></code.inline> 里 <code.inline>T</code.inline> 就像是一个『占位类型』，调用时传什么类型，T 就变成什么类型。<code.inline>identity<number>(42)</code.inline> 返回类型就是 number，<code.inline>identity(\"hello\")</code.inline> 返回类型就是 string。类型信息被完整保留，不会变成 any。" },
        { type: "h", text: "泛型接口与泛型类" },
        { type: "code", lang: "typescript", title: "泛型容器", code: '// 泛型接口\ninterface Box<T> {\n  value: T;\n}\nconst numBox: Box<number> = { value: 42 };\nconst strBox: Box<string> = { value: "hi" };\nconsole.log(numBox.value, strBox.value);\n\n// 泛型类：一个能装任意类型的栈\nclass Stack<T> {\n  private items: T[] = [];\n  push(item: T): void {\n    this.items.push(item);\n  }\n  pop(): T | undefined {\n    return this.items.pop();\n  }\n}\n\nconst stack = new Stack<number>();\nstack.push(1);\nstack.push(2);\nconsole.log(stack.pop());   // 2\nconsole.log(stack.pop());   // 1' },
        { type: "h", text: "泛型约束 extends" },
        { type: "code", lang: "typescript", title: "约束泛型的形状", code: 'interface HasLength {\n  length: number;\n}\n\n// T extends HasLength：T 必须是有 length 属性的类型\nfunction longest<T extends HasLength>(a: T, b: T): T {\n  return a.length >= b.length ? a : b;\n}\n\nconsole.log(longest("hello", "world!"));   // 字符串有 length\nconsole.log(longest([1, 2], [1, 2, 3]));   // 数组有 length\n// console.log(longest(1, 2));  // 报错：数字没有 length' },
        { type: "p", text: "<code.inline>T extends HasLength</code.inline> 表示：T 必须是满足 <code.inline>HasLength</code.inline> 形状的类型（有 length 属性）。这样在泛型内部就能安全地访问 <code.inline>a.length</code.inline>。约束让泛型既通用又有边界。" },
        { type: "warn", title: "泛型不是 any", text: "泛型保留了类型关系，而 any 把类型关系全部抹掉。比如 <code.inline>identity<number>(42)</code.inline> 的返回值确定是 number，编译器能继续帮你检查；用 any 就全没了。能用泛型就绝不用 any。" },
        { type: "keypoints", items: ["泛型用类型参数 T 代表未知类型，调用时确定", "泛型函数/接口/类都能写", "T extends 约束泛型的形状，保证安全访问", "泛型保留类型关系，不是 any", "数组操作（reverse、first 等）常用泛型"] },
      ],
      templates: [
        { name: "取数组第一个元素", code: 'function firstElement<T>(arr: T[]): T | undefined {\n  return arr[0];\n}\n\nconsole.log(firstElement([1, 2, 3]));\nconsole.log(firstElement(["a", "b", "c"]));\nconsole.log(firstElement([]));  // undefined\n\n// 类型自动推断：返回值分别是 number | undefined 和 string | undefined' },
      ],
    },
    {
      id: "ts-10",
      title: "类型收窄与守卫：typeof / in / instanceof / as",
      summary: "在联合类型里判断具体是哪一种，让代码更安全。",
      difficulty: "进阶",
      blocks: [
        { type: "p", text: "联合类型表示『可能是 A 也可能是 B』。但用的时候你常常需要知道**当前到底是哪一个**。类型收窄（Narrowing）就是通过条件判断，让 TS 在某个代码块里『缩小』类型范围，从而安全地访问该类型的专属属性和方法。" },
        { type: "h", text: "typeof 收窄" },
        { type: "code", lang: "typescript", title: "typeof 判断基础类型", code: 'function formatValue(v: string | number): string {\n  // 这里 v 可能是 string 也可能是 number\n  if (typeof v === "string") {\n    // 在这个分支里，v 被收窄为 string\n    return v.toUpperCase();\n  } else {\n    // 剩余情况，v 被收窄为 number\n    return v.toFixed(2);\n  }\n}\n\nconsole.log(formatValue("hello"));   // HELLO\nconsole.log(formatValue(3.14159));   // 3.14' },
        { type: "p", text: "在 <code.inline>if (typeof v === \"string\")</code.inline> 分支内，TS 知道 v 一定是 string，所以可以放心调用 <code.inline>toUpperCase()</code.inline>。这就是收窄：**通过判断把大类型缩小成小类型**。" },
        { type: "h", text: "in 与 instanceof" },
        { type: "code", lang: "typescript", title: "判断对象与类", code: 'interface Fish { swim(): void }\ninterface Bird { fly(): void }\n\nfunction move(animal: Fish | Bird): void {\n  // in 判断是否有某个属性\n  if ("swim" in animal) {\n    animal.swim();\n  } else {\n    animal.fly();\n  }\n}\n\n// instanceof 判断类的实例\nclass Dog { bark() { console.log("汪汪"); } }\nclass Cat { meow() { console.log("喵喵"); } }\n\nfunction speak(pet: Dog | Cat): void {\n  if (pet instanceof Dog) {\n    pet.bark();\n  } else {\n    pet.meow();\n  }\n}\n\nconst dog = new Dog();\nspeak(dog);   // 汪汪' },
        { type: "h", text: "类型断言 as" },
        { type: "code", lang: "typescript", title: "断言：我比你更懂类型", code: '// 场景：从 DOM 里拿元素，TS 只知道是 HTMLElement | null\nconst input = document.getElementById("myInput") as HTMLInputElement;\n// 断言成 HTMLInputElement 后，就能访问 value\ninput.value = "你好";\n\n// 联合类型断言成更具体的类型\nconst value: unknown = "hello";\nconst len = (value as string).length;\nconsole.log(len);   // 5\n\n// 双重断言（危险，别乱用）\n// const n = (value as unknown as number);' },
        { type: "danger", title: "as 是信任声明，不是转换", text: "类型断言 <code.inline>as</code.inline> 只是告诉 TS『相信我，它是这个类型』，**不会在运行时改变任何东西**。如果断言错了，TS 不会帮你发现，运行时会出问题。能用收窄（typeof/in/instanceof）就别用断言，断言是最后手段。" },
        { type: "keypoints", items: ["收窄：通过判断把联合类型缩小到具体类型", "typeof 收窄基础类型，in 收窄对象属性，instanceof 收窄类", "类型断言 as 告诉编译器『我确定是这个类型』", "断言不改变运行时行为，断言错误 TS 无法发现", "优先用收窄，断言是最后手段"] },
      ],
      templates: [
        { name: "智能格式化", code: 'function printValue(v: string | number | boolean): string {\n  if (typeof v === "string") return `字符串：${v.toUpperCase()}`;\n  if (typeof v === "number") return `数字：${v.toFixed(2)}`;\n  return `布尔：${v ? "真" : "假"}`;\n}\n\nconsole.log(printValue("hi"));\nconsole.log(printValue(3.14159));\nconsole.log(printValue(true));' },
      ],
    },
    {
      id: "ts-11",
      title: "模块化：export / import / 默认导出",
      summary: "把代码拆成多个文件，用 import/export 组织依赖。",
      difficulty: "进阶",
      blocks: [
        { type: "p", text: "真实项目不可能写在一个文件里。**模块化**就是把代码按功能拆成多个文件，每个文件通过 <code.inline>export</code.inline> 导出对外提供的东西，其他文件用 <code.inline>import</code.inline> 导入使用。这让代码更清晰、可复用、避免命名冲突。" },
        { type: "h", text: "命名导出与导入" },
        { type: "code", lang: "typescript", title: "math.ts 导出多个", code: '// ===== math.ts =====\nexport function add(a: number, b: number): number {\n  return a + b;\n}\n\nexport const PI = 3.14159;\n\nexport interface Result {\n  sum: number;\n  count: number;\n}\n\n// ===== 在别的文件里导入 =====\n// import { add, PI, Result } from "./math";\n// console.log(add(1, 2));\n// console.log(PI);' },
        { type: "p", text: "<code.inline>export</code.inline> 可以让函数、常量、类型（interface/type）被外部使用。导入时用花括号包住需要的名字：<code.inline>import { add, PI } from \"./math\"</code.inline>。注意导入路径一般**不带 .ts 后缀**（编译后是 .js，模块系统自动处理）。" },
        { type: "h", text: "默认导出与重命名" },
        { type: "code", lang: "typescript", title: "默认导出与 as 重命名", code: '// ===== logger.ts =====\nexport default function log(msg: string): void {\n  console.log("[LOG]", msg);\n}\n\n// ===== 导入默认导出（名字可自取）=====\n// import log from "./logger";\n// log("这是一条日志");\n\n// ===== 重命名导入 =====\n// import { add as plus } from "./math";\n// console.log(plus(1, 2));\n\n// ===== 全部导入 =====\n// import * as math from "./math";\n// console.log(math.PI);' },
        { type: "p", text: "每个模块只能有一个 <code.inline>export default</code.inline>（默认导出），导入时**名字可以随便取**，不用花括号。用 <code.inline>as</code.inline> 可以重命名导入，用 <code.inline>import * as math</code.inline> 可以把一个模块的所有导出装进一个命名空间。" },
        { type: "h", text: "重导出与类型导入" },
        { type: "code", lang: "typescript", title: "re-export 与 type", code: '// ===== index.ts：把多个模块统一导出 =====\n// export * from "./math";\n// export { default as log } from "./logger";\n\n// 这样使用者只需要：import { add, log } from "./index";\n\n// ===== 只导入类型（编译时会被擦除）=====\n// import type { Result } from "./math";\n// import { add } from "./math";   // 值用普通 import' },
        { type: "warn", title: "值和类型要分清", text: "用 <code.inline>import type</code.inline> 可以只导入类型（interface/type），编译后会被擦掉，不会产生运行时依赖，还能避免循环引用问题。如果导入的是真正的值（函数/常量/类），用普通 <code.inline>import</code.inline>。" },
        { type: "info", title: "tsconfig 模块配置", text: "模块相关的关键配置：<code.inline>module</code.inline>（模块系统，Node 后端常用 commonjs，前端打包用 esnext/ES2020）、<code.inline>moduleResolution</code.inline>（模块解析策略）。默认生成的 tsconfig 即可，遇到报错再按提示调整。" },
        { type: "keypoints", items: ["export 导出、import 导入，模块化组织代码", "命名导出用花括号，默认导出每模块一个", "as 重命名，import * as 导入全部", "export * 重导出统一入口", "类型用 import type 导入，编译后会被擦除"] },
      ],
      templates: [
        { name: "数学工具模块", code: '// 模拟模块：下面的内容相当于一个 math.ts\nexport function square(n: number): number {\n  return n * n;\n}\nexport function cube(n: number): number {\n  return n * n * n;\n}\n\n// 在真实项目中：import { square } from "./math";\nconsole.log(square(5));   // 25\nconsole.log(cube(3));      // 27' },
      ],
    },
    {
      id: "ts-12",
      title: "内置工具类型：Partial/Pick/Omit/Record/ReturnType",
      summary: "用 TS 内置的泛型工具快速变换类型，认识 satisfies。",
      difficulty: "进阶",
      blocks: [
        { type: "p", text: "TS 内置了一批**工具类型**，用泛型帮你快速变换类型，避免手写重复结构。最常用的有 Partial、Pick、Omit、Record、ReturnType 等。它们都是『类型工厂』：传入一个类型，返回一个新的类型。" },
        { type: "h", text: "Partial / Pick / Omit" },
        { type: "code", lang: "typescript", title: "变换对象类型", code: 'interface User {\n  id: number;\n  name: string;\n  email: string;\n}\n\n// Partial：所有属性变成可选\n// 常用于表单：用户可能只改其中几项\ntype PartialUser = Partial<User>;\nconst patch: PartialUser = { name: "新名字" };   // 合法\n\n// Pick：挑选部分属性\n// 常用于只展示需要的字段\ntype UserSummary = Pick<User, "id" | "name">;\nconst summary: UserSummary = { id: 1, name: "小明" };\n\n// Omit：排除部分属性\ntype UserWithoutEmail = Omit<User, "email">;\nconst u: UserWithoutEmail = { id: 2, name: "小红" };\n\nconsole.log(patch, summary, u);' },
        { type: "p", text: "<code.inline>Partial<User></code.inline> 把 User 的所有属性变成可选；<code.inline>Pick<User, \"id\" | \"name\"></code.inline> 只保留指定属性；<code.inline>Omit<User, \"email\"></code.inline> 去掉指定属性。它们三个覆盖了最常见的『改类型』需求。" },
        { type: "h", text: "Record / ReturnType / satisfies" },
        { type: "code", lang: "typescript", title: "映射与函数类型", code: '// Record：用一组键映射到同一类型\n// 适合状态表、配置表\ntype Role = "admin" | "user" | "guest";\ntype Permissions = Record<Role, boolean>;\nconst perms: Permissions = {\n  admin: true,\n  user: false,\n  guest: false,\n};\nconsole.log(perms.admin);\n\n// ReturnType：取函数返回值的类型\nfunction makeUser() {\n  return { id: 1, name: "小明" };\n}\ntype MadeUser = ReturnType<typeof makeUser>;\nconst mu: MadeUser = { id: 2, name: "小红" };\nconsole.log(mu);' },
        { type: "h", text: "satisfies：保留字面量又不丢检查" },
        { type: "code", lang: "typescript", title: "satisfies 用法", code: 'interface Config {\n  path: string;\n  strict: boolean;\n}\n\n// 普通标注：写死类型\nconst c1: Config = { path: "/api", strict: true };\n\n// satisfies：既检查满足 Config，又保留具体字面量类型\nconst c2 = { path: "/api", strict: true } satisfies Config;\n\n// c1.path 类型是 string，c2.path 类型是 "/api"（更精确）\nconsole.log(c1.path, c2.path);' },
        { type: "warn", title: "工具类型都只能用在类型位置", text: "<code.inline>Partial</code.inline>、<code.inline>Pick</code.inline> 这些是**类型层面的操作**，只能写在类型标注里（<code.inline>: PartialUser</code.inline> 或 <code.inline>type X = ...</code.inline>），不能在运行时当函数调用。写错位置会得到『Cannot find name』之类的报错。" },
        { type: "keypoints", items: ["Partial 全部可选、Pick 挑选、Omit 排除属性", "Record<K,V> 用键映射类型，适合配置表/状态表", "ReturnType 取函数返回值类型", "satisfies 检查形状又保留字面量类型", "工具类型只能用于类型位置，不是运行时的函数"] },
      ],
      templates: [
        { name: "表单状态管理器", code: 'interface FormData {\n  username: string;\n  password: string;\n  email: string;\n}\n\n// 编辑表单时，字段都是可选的\nconst form: Partial<FormData> = {\n  username: "小明",\n};\n\n// 提交前检查必填字段\ntype RequiredFields = Pick<FormData, "username" | "password">;\nconst required: RequiredFields = {\n  username: form.username ?? "",\n  password: form.password ?? "",\n};\n\nconsole.log(required);' },
      ],
    },
    {
      id: "ts-13",
      title: "与 JavaScript 互操作：.d.ts 声明文件",
      summary: "让 TS 认识 JS 代码，掌握 declare 与 @types 类型包。",
      difficulty: "进阶",
      blocks: [
        { type: "p", text: "现实世界不可能全是 TS：有的是老代码、有的是第三方 JS 库。**声明文件（.d.ts）** 就是『用 TS 语法描述一个 JS 模块的类型』的文件，它只含类型不含实现，让 TS 能检查 JS 代码。" },
        { type: "h", text: "先看现象：JS 库没类型" },
        { type: "code", lang: "typescript", title: "缺类型时报错", code: '// 假设装了第三方 JS 库 lodash\nimport _ from "lodash";\n\n// 没有类型声明时，TS 会报错：\n// 找不到模块 "lodash" 或其相应的类型声明。\n\n// 解决办法：安装官方维护的类型包\n// npm install --save-dev @types/lodash\n\n// 装完后 TS 就认识这个库了\nconst arr = _.chunk([1, 2, 3, 4, 5], 2);\nconsole.log(arr);' },
        { type: "p", text: "很多流行的 JS 库（lodash、express、react 等）都有社区维护的类型包，统一放在 <code.inline>@types</code.inline> 名下。安装方式：<code.inline>npm install --save-dev @types/包名</code.inline>。这是让 TS 认识第三方 JS 库的**首选方案**，别自己手写。" },
        { type: "h", text: "declare 声明" },
        { type: "code", lang: "typescript", title: "手写声明文件", code: '// ===== my-module.d.ts =====\n// 声明一个模块及其导出\n// 文件里不能有 import/export 时，declare 就是全局声明\ndeclare module "my-js-lib" {\n  export function doSomething(x: number): string;\n  export const version: string;\n}\n\n// ===== 在 TS 里使用 =====\n// import { doSomething } from "my-js-lib";\n// console.log(doSomething(42));\n\n// 声明全局变量（没有模块系统时用）\n// declare const API_URL: string;' },
        { type: "p", text: "当第三方库没有现成的 @types 包时，你可以自己写一个 <code.inline>.d.ts</code.inline> 文件用 <code.inline>declare module \"库名\"</code.inline> 描述它的导出。这是兜底方案，能解决『找不到模块类型』的报错。" },
        { type: "h", text: "在 TS 里直接写 JS 代码" },
        { type: "code", lang: "typescript", title: "any 逃生与导入 JS", code: '// 1. 允许 .js 文件参与编译\n// tsconfig.json 里：\n//   "allowJs": true\n\n// 2. 对暂时没类型的 JS 值，用 any 逃生\n// const result: any = someJsFunction();\n\n// 3. 检查一个 .js 文件是否被允许\n// tsc 时加上：--allowJs' },
        { type: "warn", title: "别把声明文件和实现文件搞混", text: "<code.inline>.d.ts</code.inline> 只描述类型，编译后**不会生成任何 JS**；<code.inline>.ts</code.inline> 有实现，会编译成 JS。往 .d.ts 里写业务逻辑是错误用法。" },
        { type: "keypoints", items: [".d.ts 声明文件只含类型不含实现", "第三方 JS 库优先装 @types 类型包", "declare module 手写声明第三方库", "allowJs 允许 TS 项目里混用 JS 文件", "老项目可以渐进式迁移到 TS"] },
      ],
      templates: [
        { name: "手写一个小声明", code: '// 演示：为自己写一个简单的声明模块\ndeclare module "my-utils" {\n  export function formatDate(d: Date): string;\n  export const VERSION: string;\n}\n\n// 实际使用（真实项目里写在别的文件）：\n// import { formatDate } from "my-utils";\n// console.log(formatDate(new Date()));\nconsole.log("声明文件学习完成");' },
      ],
    },
    {
      id: "ts-14",
      title: "在 React 中使用 TypeScript",
      summary: "给组件 props 加类型，useState 泛型，事件处理类型化。",
      difficulty: "进阶",
      blocks: [
        { type: "p", text: "React 是目前最流行的前端框架之一，和 TS 是绝配。用 TS 写 React，组件接收什么 props、state 是什么类型都一目了然，重构时也更安心。这一章讲最常用的几个类型化技巧。" },
        { type: "h", text: "用模板创建 React + TS 项目" },
        { type: "code", lang: "typescript", title: "Vite 创建 React-TS 项目", code: '# 用 Vite 创建 React + TypeScript 项目（推荐）\nnpm create vite@latest my-app -- --template react-ts\n\n# 进入项目并安装依赖\ncd my-app\nnpm install\n\n# 启动开发服务器\nnpm run dev' },
        { type: "h", text: "组件 props 类型化" },
        { type: "code", lang: "typescript", title: "函数组件的 props", code: '// 定义一个 props 类型\ntype GreetingProps = {\n  name: string;\n  age?: number;          // 可选 props\n  onSayHi: () => void;   // 回调函数 props\n};\n\n// 组件接收并标注 props 类型\nfunction Greeting({ name, age, onSayHi }: GreetingProps) {\n  return (\n    <div>\n      <h1>你好，{name}！</h1>\n      {age !== undefined && <p>今年 {age} 岁</p>}\n      <button onClick={onSayHi}>打招呼</button>\n    </div>\n  );\n}\n\nexport default Greeting;\n\n// 使用：<Greeting name="小明" onSayHi={() => alert("嗨")} />' },
        { type: "p", text: "用 interface 或 type 定义 props 形状，组件参数标注成这个类型。这样在使用组件时，少传 props 或多传类型不对的 props，编辑器都会报错——这是 React + TS 最直接的收益。" },
        { type: "h", text: "useState 与事件类型" },
        { type: "code", lang: "typescript", title: "useState 泛型与事件", code: 'import { useState } from "react";\n\n// useState 的泛型：明确 state 类型\ntype Todo = { id: number; text: string; done: boolean };\n\nfunction TodoApp() {\n  const [todos, setTodos] = useState<Todo[]>([]);\n  const [input, setInput] = useState("");\n\n  // 事件对象用类型标注\n  function handleChange(e: React.ChangeEvent<HTMLInputElement>) {\n    setInput(e.target.value);\n  }\n\n  function addTodo() {\n    setTodos([...todos, { id: Date.now(), text: input, done: false }]);\n    setInput("");\n  }\n\n  return (\n    <div>\n      <input value={input} onChange={handleChange} />\n      <button onClick={addTodo}>添加</button>\n      <ul>\n        {todos.map((t) => (\n          <li key={t.id}>{t.text}</li>\n        ))}\n      </ul>\n    </div>\n  );\n}\n\nexport default TodoApp;' },
        { type: "info", title: "常用事件类型", text: "<code.inline>React.ChangeEvent<HTMLInputElement></code.inline> 是输入框的 change 事件；<code.inline>React.MouseEvent<HTMLButtonElement></code.inline> 是按钮点击事件；<code.inline>React.FormEvent</code.inline> 是表单提交事件。鼠标悬停在事件参数上，VS Code 会告诉你该写什么类型。" },
        { type: "warn", title: "tsx 文件里的泛型陷阱", text: "在 <code.inline>.tsx</code.inline> 文件里写 <code.inline>useState<string></code.inline> 没问题，但写 <code.inline><T,>(...)</code.inline> 这类泛型箭头函数时，尖括号会被 JSX 语法混淆，需要加逗号 <code.inline><T,></code.inline>。遇到解析错误先往这个方向排查。" },
        { type: "keypoints", items: ["Vite 的 react-ts 模板一键创建 TS 项目", "用 interface/type 定义组件 props 类型", "useState<Todo[]>([]) 用泛型明确 state 类型", "事件对象用 React.ChangeEvent / MouseEvent 标注", "tsx 里泛型箭头函数要写 <T,> 避免歧义"] },
      ],
      templates: [
        { name: "按钮组件（类型化 props）", code: 'type ButtonProps = {\n  label: string;\n  variant?: "primary" | "secondary";\n  disabled?: boolean;\n  onClick: () => void;\n};\n\nfunction MyButton({ label, variant = "primary", disabled, onClick }: ButtonProps) {\n  const style = variant === "primary" ? "blue" : "gray";\n  return (\n    <button style={{ background: style }} disabled={disabled} onClick={onClick}>\n      {label}\n    </button>\n  );\n}\n\nexport default MyButton;\n// 使用：<MyButton label="提交" onClick={() => console.log("点击")} />' },
      ],
    },
    {
      id: "ts-15",
      title: "在 Node.js / Express 中使用 TypeScript",
      summary: "搭建 TS 后端，用 tsx 运行，给 Express 路由加类型。",
      difficulty: "高级",
      blocks: [
        { type: "p", text: "TS 不只用于前端，Node.js 后端同样能享受类型安全。用 Express 写 Web API 时，把请求参数、响应数据都类型化，接口契约一目了然。这一章教你搭一个最小的 TS + Express 后端。" },
        { type: "h", text: "初始化项目并装依赖" },
        { type: "code", lang: "typescript", title: "搭建 TS 后端", code: '# 创建项目并初始化\nnpm init -y\n\n# 安装 Express 和类型\nnpm install express\nnpm install --save-dev typescript tsx @types/express @types/node\n\n# 生成 tsconfig\nnpx tsc --init\n\n# 运行（tsx 直接跑 .ts，无需先编译）\nnpx tsx src/index.ts' },
        { type: "p", text: "<code.inline>tsx</code.inline> 是一个能直接运行 .ts 文件的工具（基于 esbuild），开发时不用先 tsc 编译，改完代码立即生效，体验很好。它也是当前 TS 后端开发的主流运行方式，替代了老牌的 ts-node。" },
        { type: "h", text: "定义接口与数据类型" },
        { type: "code", lang: "typescript", title: "定义数据模型", code: '// src/types.ts\nexport interface Todo {\n  id: number;\n  title: string;\n  completed: boolean;\n}\n\n// 创建请求体：可以复用 Omit 去掉 id\nexport type CreateTodo = Omit<Todo, "id">;\n\n// src/data.ts：简单的内存数据库\nimport { Todo } from "./types";\n\nexport const todos: Todo[] = [\n  { id: 1, title: "学习 TypeScript", completed: false },\n  { id: 2, title: "写后端接口", completed: false },\n];' },
        { type: "h", text: "Express 路由类型化" },
        { type: "code", lang: "typescript", title: "路由处理", code: '// src/index.ts\nimport express, { Request, Response } from "express";\nimport { todos } from "./data";\n\nconst app = express();\napp.use(express.json());\n\n// GET /todos：返回全部\napp.get("/todos", (req: Request, res: Response) => {\n  res.json(todos);\n});\n\n// POST /todos：新增\napp.post("/todos", (req: Request, res: Response) => {\n  const body: { title: string } = req.body;\n  const newTodo = { id: Date.now(), title: body.title, completed: false };\n  todos.push(newTodo);\n  res.status(201).json(newTodo);\n});\n\n// 路径参数：req.params\napp.get("/todos/:id", (req: Request, res: Response) => {\n  const id = Number(req.params.id);\n  const found = todos.find((t) => t.id === id);\n  if (!found) {\n    res.status(404).json({ message: "未找到" });\n    return;\n  }\n  res.json(found);\n});\n\napp.listen(3000, () => {\n  console.log("服务已启动：http://localhost:3000");\n});' },
        { type: "warn", title: "req.body 类型是 any", text: "Express 的 <code.inline>req.body</code.inline> 默认类型是 <code.inline>any</code.inline>。最好显式声明 <code.inline>const body: { title: string } = req.body;</code> 或定义接口，否则就失去了类型检查的意义。想更严谨可以借助 zod 等校验库（进阶内容）。" },
        { type: "h", text: "package.json 脚本" },
        { type: "code", lang: "typescript", title: "开发与构建脚本", code: '{\n  "scripts": {\n    "dev": "tsx watch src/index.ts",       // 开发：监听自动重启\n    "build": "tsc",                        // 构建：编译成 JS 到 dist\n    "start": "node dist/index.js"          // 生产：运行编译后的 JS\n  }\n}' },
        { type: "keypoints", items: ["tsx 直接运行 .ts，开发体验好", "接口用 interface/type 定义，跨文件复用", "Express 路由给 req/res 标注类型", "req.params 是字符串，转数字用 Number()", "req.body 默认 any，要显式声明类型", "找不到资源返回 404 并 return，避免继续执行"] },
      ],
      templates: [
        { name: "迷你 HTTP 服务器", code: 'import { createServer } from "http";\n\ninterface Message {\n  code: number;\n  text: string;\n}\n\nconst data: Message = { code: 200, text: "Hello TypeScript Backend" };\n\nconst server = createServer((req, res) => {\n  res.writeHead(200, { "Content-Type": "application/json" });\n  res.end(JSON.stringify(data));\n});\n\nserver.listen(3000, () => {\n  console.log("服务器运行在 http://localhost:3000");\n});' },
      ],
    },
    {
      id: "ts-16",
      title: "工程化与实战：tsconfig / ESLint / 构建发布",
      summary: "配置生产级 tsconfig，接上 ESLint，学会排错和发布。",
      difficulty: "高级",
      blocks: [
        { type: "p", text: "最后一步，把 TS 工程配置得像生产项目：合理的 tsconfig、接入 ESLint 检查代码质量、用构建工具打包发布。这一章把常用配置和常见报错一次讲清楚。" },
        { type: "h", text: "生产级 tsconfig 常用配置" },
        { type: "code", lang: "typescript", title: "推荐配置", code: '{\n  "compilerOptions": {\n    "target": "ES2020",            // 输出目标版本\n    "module": "ESNext",            // 模块系统\n    "moduleResolution": "bundler", // 现代打包器的解析方式\n    "strict": true,                // 严格模式（必开）\n    "esModuleInterop": true,       // 兼容 CommonJS 导入\n    "skipLibCheck": true,          // 跳过 .d.ts 检查，加快编译\n    "forceConsistentCasingInFileNames": true,\n    "noEmit": true,                // 只检查不输出（配合打包器）\n    "outDir": "dist"               // 需要输出 JS 时用\n  },\n  "include": ["src"],\n  "exclude": ["node_modules", "dist"]\n}' },
        { type: "p", text: "如果是纯工具库/Node 项目，用 <code.inline>tsc</code.inline> 输出 JS；如果配合打包器（Vite/Webpack），通常 <code.inline>noEmit: true</code.inline> 只做类型检查，由打包器负责产出最终文件。" },
        { type: "h", text: "接入 ESLint + typescript-eslint" },
        { type: "code", lang: "typescript", title: "安装与配置 ESLint", code: '# 安装\nnpm install --save-dev eslint @typescript-eslint/parser @typescript-eslint/eslint-plugin\n\n# 初始化配置文件\nnpx eslint --init\n\n# 检查\nnpx eslint src/\n\n# 自动修复\nnpx eslint src/ --fix' },
        { type: "h", text: "构建与发布" },
        { type: "code", lang: "typescript", title: "完整发布流程", code: '# 1. 类型检查 + Lint\nnpx tsc --noEmit\nnpx eslint src/ --fix\n\n# 2. 构建（前端：打包静态资源）\nnpm run build   # 通常是 vite build 或 tsc + 打包器\n\n# 3. 预览产物\nnpm run preview\n\n# 4. 部署：把 dist/ 或打包产物传到服务器/CDN' },
        { type: "h", text: "常见报错速查" },
        { type: "table", head: ["报错", "原因", "解决"], rows: [["Cannot find module 'xxx' 或其类型声明", "第三方库缺类型包", "npm i -D @types/xxx"], ["Type 'X' is not assignable to type 'Y'", "类型不匹配", "检查字段类型，或用收窄/断言"], ["Property 'x' does not exist on type", "访问了不存在的属性", "确认结构定义，或先收窄类型"], ["Object is possibly 'undefined'", "可能为空值", "加 null 判断或用可选链 ?."], ["This expression is not callable", "把非函数当函数调用", "确认类型里是否有该方法"]] },
        { type: "warn", title: "改 tsconfig 报错满天飞", text: "开启 strict 或改 module 后可能冒出一堆错误，别慌。多数是历史代码不符合新配置。逐个修复，或者先看是否缺 @types 包、是否 import 路径写错。善用 VS Code 的『快速修复』（灯泡图标）能省很多事。" },
        { type: "keypoints", items: ["strict 必开，noEmit 配合打包器，outDir 用于输出", "ESLint + typescript-eslint 管代码质量（查风格和隐式 any）", "发布流程：类型检查 → lint → build → 部署", "第三方库报错先查 @types 包", "排错优先看报错信息 + VS Code 快速修复"] },
      ],
      templates: [
        { name: "总结清单：新项目开工模板", code: '// 一个最小的 TS 项目启动步骤（命令行）：\n// npm init -y\n// npm install -D typescript\n// npx tsc --init && 开启 strict\n// npm install -D eslint @typescript-eslint/parser @typescript-eslint/eslint-plugin\n// 新建 src/index.ts，写下第一行：\nconst greeting: string = "我的 TS 项目开工了！";\nconsole.log(greeting);' },
      ],
    },
  ]
});
